/*
==(checks if the values are same) > >= < <=        !=(not equal to)
1 means true
0 means false

!=(if not equal to then 1 but if equal to then 0 )
*/ 
#include <stdio.h>
#include <math.h>

int main(){

printf("%d \n", 4 != 5 );

    return 0;
}
